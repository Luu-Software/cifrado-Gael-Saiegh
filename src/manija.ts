import { preguntar } from './lib/consola.ts';

function obtenerPrefijo(numero: number, tamañoPrefijo: number): number {
  return 0; // COMPLETAR
}

let tarjetaTexto: string = preguntar('Ingresá el número de tarjeta: ');

let red: string = ''; // COMPLETAR
console.log(red);

let tarjeta: string= preguntar('indique su tarjeta')
let tarnumtext: string= preguntar('indique el numero de su tarjeta')
let tarnum= Number(tarnumtext)

if (accion= 'obtenerprefijo') {
  if (tarjeta= 'visa') {
    if (tarnum= '4') {
        resultado = obtenerprefijo (tarnum)
    }
 }
  }